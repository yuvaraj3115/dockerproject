version: 2
jobs:
  docker_build:
    machine: true
    steps:
      - checkout
      - run: cd ./
      - run: docker build -t spine:1.0 .
      - run: docker login -u "${DOCKER_USER}" -p "${DOCKER_PASS}"
      - run:
          name: Tag and push to docker hub
          command: |
           docker push spine:1.0

vultr_deploy:
    machine: true
    steps:
      - run: 
          ssh ubuntu@13.250.17.114

workflows:
  version: 2
  rolling_update:
    jobs:
      - docker_build
      - vultr_deploy:
          requires:
            - docker_build
          filters:
            branches:
              only:
                - master

