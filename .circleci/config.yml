version: 2
jobs:
  docker_build:
    machine: true
    steps:
      - checkout
      - run: docker build -t spine:1.0 ./    
      - run: docker login -u "${DOCKER_USER}" -p "${DOCKER_PASS}"
      - run: docker push yuvarajnbkr2015/spine:1.0
     
     
vultr_deploy:
    machine: true
    steps:
      - run: |
          ssh ubunntu@13.250.17.114 
          
workflows:
  version: 2
  rolling_update:
    jobs:
      - docker_build
      - vultr_deploy:
          requires:
            - docker_build
          filters:
            branches:
              only:
                - master
        
  
 


  
             




    



                     
